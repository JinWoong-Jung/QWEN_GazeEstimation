# Libraries

- `model/datasets.py`
  - function format_target_text: (label_text, label_id, id2label, str] | None, vocab2id, int] | None, vocab2id_lower, int] | None, num_classes, answer_template, fallback_target_text, point_x, point_y, point_decimals) -> tuple[str, float]
  - function draw_head_bbox_prompt: (scene, x1, y1, x2, y2) -> Image.Image
  - class GazeDataset
  - class GazeTestDataset
- `model/model.py` — class QwenTextGenerationModel
- `model/modules/preprocess.py`
  - function size_tuple: (size, int]) -> tuple[int, int]
  - function resize_tensor_image: (image, size, int], mode, antialias) -> torch.Tensor
  - function resize_pil_image: (image, size, int], resample) -> Image.Image
  - function resize_any: (image, size, int]) -> Any
  - class GazeInputResizer
- `model/trainer.py`
  - function resolve_path: (path) -> Path
  - function set_seed: (seed) -> None
  - function parse_dtype: (dtype) -> torch.dtype | str
  - function to_autocast_dtype: (dtype) -> torch.dtype
  - function build_id2label: (vocab2id, int]) -> dict[int, str]
  - function count_valid_targets: (records) -> int
  - _...9 more_
- `model/utils/checkpoint.py`
  - function resolve_qwen_model: (model) -> Any
  - function save_checkpoint: (ckpt_dir, epoch, model, processor, optimizer, scheduler, clear_dir) -> None
  - function load_checkpoint_for_eval: (ckpt_dir, model, device) -> bool
- `model/utils/common.py`
  - function to_device: (batch, Any], device) -> dict[str, Any]
  - function normalize_text: (text) -> str
  - function chat_text: (processor, user_text, assistant_text, *, with_image, add_generation_prompt) -> str
- `model/utils/config_parser.py`
  - function flatten_config: (raw, Any]) -> dict[str, Any]
  - function load_yaml_config: (config_path) -> dict[str, Any]
  - function default_value: (defaults, Any], key, fallback) -> Any
  - function build_parser: (defaults, Any] | None) -> argparse.ArgumentParser
- `model/utils/data_utils.py`
  - function is_normalized_point: (x, y) -> bool
  - function sanitize_bbox_pixels: (bbox, float, float, float], width, height) -> tuple[int, int, int, int]
  - function build_prompt: (bbox_norm, float, float, float], prompt_template, prompt_text) -> str
  - function clamp01: (x) -> float
  - function expand_bbox: (bbox_px, float, float, float], width, height, scale) -> tuple[float, float, float, float]
  - function safe_crop: (scene, gaze_x, gaze_y, bbox_px, float, float, float], p) -> tuple[Image.Image, float, float, tuple[float, float, float, float]]
  - _...20 more_
- `model/utils/eval_utils.py`
  - function parse_object_text: (text) -> str | None
  - function parse_point: (text) -> tuple[float, float] | None
  - function decode_generated: (processor, generated_ids, input_ids, attention_mask, num_return_sequences) -> list[str]
  - function l2_stats: (pred_xy, float], gt_points) -> tuple[float, float] | None
  - function topk_similarity: (query, bank, k, temperature) -> list[int]
  - function run_eval: (model, loader, device, amp_dtype, loss_answer_weight, show_tqdm, desc) -> dict[str, float]
  - _...3 more_
- `model/utils/label_bank.py` — function canonicalize: (text) -> str, class LabelBank
- `model/utils/loss_utils.py` — function masked_token_ce: (logits, labels, mask) -> tuple[torch.Tensor, int], function compute_answer_loss: (*, logits, labels, loss_mask_answer, weight_answer) -> dict[str, Any]
- `model/utils/object_tokens.py`
  - function add_slot_token: (tokenizer) -> int
  - function slot_span: (text) -> tuple[int, int] | None
  - function object_label_span: (text) -> tuple[int, int] | None
- `model/utils/processor_collate.py`
  - function mask_padding_labels: (input_ids, attention_mask, pad_token_id) -> torch.Tensor
  - function find_subseq: (seq, sub, *, from_right) -> int
  - function build_answer_mask: (processor, joint_inputs, Any], target_texts, target_valid) -> torch.Tensor
  - function build_train_inputs: (processor, scene_images, text_inputs, target_texts, target_valid, max_text_length) -> tuple[dict[str, Any], torch.Tensor, torch.Tensor]
  - function build_infer_inputs: (processor, scene_images, text_inputs, max_text_length) -> dict[str, Any]
  - class QwenTrainCollator
  - _...1 more_
- `model/utils/wandb_utils.py`
  - function parse_tags: (raw) -> list[str]
  - function init_wandb: (args, root)
  - function finish_wandb: (run) -> None
